/zenith
/zenith-debug
/zenith-base
*.o

# Python training environment (not source; recreated via trainer/requirements)
/.venv/

# Training artifacts: self-play datasets and net checkpoints (large / regenerable)
/data/
/nets/
__pycache__/
*.pyc

# NNUE nets are built by the trainer; commit a chosen release net explicitly with `git add -f`
*.nnue

# fastchess writes this into the working directory when running matches
/config.json
